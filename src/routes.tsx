import { BrowserRouter, Route, Routes } from 'react-router-dom'

function PageRoutes(){
  return(
    <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  )
}

export default PageRoutes